import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("1")
            Text("111")
        }
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
